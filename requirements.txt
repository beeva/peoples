# Python dependencies for server.py.
#
#   pip install -r requirements.txt      (or: npm run setup)
#
# PyMySQL is a pure-Python MySQL/MariaDB client -- no compiler, no MySQL client
# libraries needed. It is the only runtime dependency; everything else the
# server does still uses the standard library.
PyMySQL>=1.1.1
